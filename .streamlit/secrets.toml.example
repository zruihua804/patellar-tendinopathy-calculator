[app]
# Set this before entering real patient information in a deployed environment.
clinical_access_code = "replace-with-a-team-controlled-secret"

[feishu]
# Create the Base under the clinician/organization account, add the app as a collaborator,
# then provide a Base/Wiki link or its App Token. Never commit the real values.
app_id = ""
app_secret = ""
bitable_url = ""
